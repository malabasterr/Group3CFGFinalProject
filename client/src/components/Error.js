import React from "react"

function Error() {
  return (
  <h1>Page not found!</h1>
  );
};

export default Error;

// This page is to ensure if the url extension is incorrect/does not exist, an error page displays